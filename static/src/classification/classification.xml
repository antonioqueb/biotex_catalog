<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="biotex_catalog.ClassificationWorkspace">
    <div class="o_action o_bcw">

        <!-- ================================================== encabezado -->
        <header class="o_bcw_top">
            <div class="o_bcw_top_titles">
                <h1>Clasificador por Grupos</h1>
                <p>Clasifica productos de forma rápida y ordenada</p>
            </div>
            <div class="o_bcw_top_actions">
                <span t-if="state.busy" class="o_bcw_busy"><i class="fa fa-circle-o-notch fa-spin"/></span>
                <button class="btn btn-sm btn-link o_bcw_link" t-on-click="cancel">Cancelar</button>
                <button class="btn btn-sm btn-primary" t-on-click="saveAndExit">Guardar y salir</button>
            </div>
        </header>

        <div t-if="state.loading" class="o_bcw_loading"><i class="fa fa-circle-o-notch fa-spin fa-2x"/></div>

        <div t-else="" class="o_bcw_body">

            <!-- borradores pendientes: "Guardar y salir" tiene sentido solo si se puede retomar -->
            <div t-if="state.drafts.length and !state.session" class="o_bcw_drafts">
                <i class="fa fa-history"/>
                <span>Tienes sesiones sin terminar:</span>
                <t t-foreach="state.drafts" t-as="draft" t-key="draft.id">
                    <button class="o_bcw_draft_chip" t-on-click="() => this.openDraft(draft.id)">
                        <t t-esc="draft.class_code or 'Sin clasificación'"/>
                        <span class="o_bcw_draft_count"><t t-esc="draft.line_count"/></span>
                    </button>
                </t>
            </div>

            <!-- ============================================== etapa 1 -->
            <section class="o_bcw_stage" t-att-class="{o_bcw_stage_active: state.stage === 1}">
                <header class="o_bcw_stage_head" t-on-click="() => this.goStage(1)">
                    <span class="o_bcw_num o_bcw_tone_group">1</span>
                    <div class="o_bcw_stage_titles">
                        <h2>Selecciona clasificación</h2>
                        <p>Grupo, familia, clasificador y marca. Cada nivel limita el siguiente.</p>
                    </div>
                    <div class="o_bcw_stage_mini" t-if="state.collapsed[1] and classificationComplete">
                        <span class="o_bcw_code o_bcw_code_sm">
                            <t t-foreach="codeSegments" t-as="seg" t-key="seg_index">
                                <span t-attf-class="o_bcw_seg o_bcw_tone_{{seg.tone}}" t-esc="seg.text"/>
                                <span t-if="!seg_last" class="o_bcw_dash">-</span>
                            </t>
                        </span>
                        <span class="o_bcw_chip o_bcw_chip_ok"><i class="fa fa-check"/> Completa</span>
                    </div>
                    <button class="o_bcw_collapse" t-att-class="{o_bcw_collapse_open: !state.collapsed[1]}"
                            t-on-click.stop="() => this.toggleCollapse(1)"
                            t-att-title="state.collapsed[1] ? 'Desplegar etapa 1' : 'Colapsar etapa 1'">
                        <i class="fa fa-angle-left"/>
                    </button>
                </header>

                <div class="o_bcw_stage_body" t-if="!state.collapsed[1]">
                    <div class="o_bcw_levels">
                        <t t-foreach="levels" t-as="level" t-key="level.key">
                            <t t-set="sel" t-value="selection(level.key)"/>
                            <button class="o_bcw_pick"
                                    t-attf-class="o_bcw_tone_{{level.tone}} {{ sel ? 'o_bcw_picked' : '' }} {{ state.openPicker === level.key ? 'o_bcw_open' : '' }}"
                                    t-att-disabled="!levelEnabled(level.key) or confirmed"
                                    t-on-click="() => this.togglePicker(level.key)">
                                <span class="o_bcw_pick_icon"><i t-attf-class="fa {{level.icon}}"/></span>
                                <span class="o_bcw_pick_body">
                                    <span class="o_bcw_pick_label"><t t-esc="level.label"/></span>
                                    <span t-if="sel" class="o_bcw_pick_code"><t t-esc="sel.code"/></span>
                                    <span t-if="sel" class="o_bcw_pick_name" t-att-title="sel.name"><t t-esc="sel.name"/></span>
                                    <span t-if="sel and sel.note" class="o_bcw_pick_note" t-esc="sel.note"/>
                                    <span t-if="!sel" class="o_bcw_pick_empty">
                                        <t t-if="levelEnabled(level.key)">Sin seleccionar</t>
                                        <t t-else="">Elige antes <t t-esc="previousLabel(level.key)"/></t>
                                    </span>
                                </span>
                                <span class="o_bcw_pick_state"><i t-if="sel" class="fa fa-check-circle"/></span>
                                <span class="o_bcw_pick_toggle"><i class="fa fa-angle-left"/></span>
                            </button>
                        </t>
                    </div>

                    <!-- panel de opciones del nivel abierto -->
                    <div t-if="state.openPicker" class="o_bcw_picker" t-attf-class="o_bcw_tone_{{openLevel.tone}}">
                        <div class="o_bcw_picker_head">
                            <strong>Elige <t t-esc="openLevel.label.toLowerCase()"/></strong>
                            <input type="text" class="o_bcw_input" placeholder="Filtrar por código o nombre..."
                                   t-att-value="state.pickerSearch" t-on-input="onPickerSearch" autofocus="autofocus"/>
                            <button class="o_bcw_icon_btn" t-on-click="() => this.togglePicker(state.openPicker)" title="Cerrar"><i class="fa fa-times"/></button>
                        </div>
                        <div class="o_bcw_picker_list">
                            <t t-set="opts" t-value="options(state.openPicker)"/>
                            <button t-foreach="opts" t-as="opt" t-key="opt.id" class="o_bcw_opt"
                                    t-att-class="{o_bcw_opt_active: state.pick[state.openPicker] === opt.id}"
                                    t-on-click="() => this.select(state.openPicker, opt)">
                                <span class="o_bcw_opt_code"><t t-esc="opt.code"/></span>
                                <span class="o_bcw_opt_name"><t t-esc="opt.name"/></span>
                                <span t-if="opt.note" class="o_bcw_opt_note"><t t-esc="opt.note"/></span>
                                <i t-if="state.pick[state.openPicker] === opt.id" class="fa fa-check o_bcw_opt_check"/>
                            </button>
                            <div t-if="!opts.length" class="o_bcw_empty">No hay opciones disponibles en este nivel.</div>
                        </div>
                    </div>

                    <!-- resumen de clasificación -->
                    <div class="o_bcw_summary" t-att-class="{o_bcw_summary_ok: classificationComplete}">
                        <div class="o_bcw_summary_head">
                            <span class="o_bcw_summary_title">Clasificación seleccionada</span>
                            <span t-if="classificationComplete" class="o_bcw_chip o_bcw_chip_ok"><i class="fa fa-check"/> Completa</span>
                            <span t-else="" class="o_bcw_chip o_bcw_chip_pending">Faltan niveles</span>
                        </div>
                        <div class="o_bcw_code">
                            <t t-if="codeSegments.length">
                                <t t-foreach="codeSegments" t-as="seg" t-key="seg_index">
                                    <span t-attf-class="o_bcw_seg o_bcw_tone_{{seg.tone}}" t-esc="seg.text"/>
                                    <span t-if="!seg_last" class="o_bcw_dash">-</span>
                                </t>
                            </t>
                            <span t-else="" class="o_bcw_code_empty">La clave aparece aquí al completar los cuatro niveles</span>
                        </div>
                        <div class="o_bcw_summary_rows">
                            <t t-foreach="levels" t-as="level" t-key="level.key">
                                <t t-set="sel" t-value="selection(level.key)"/>
                                <div class="o_bcw_summary_row" t-attf-class="o_bcw_tone_{{level.tone}}">
                                    <span class="o_bcw_dot"/>
                                    <span class="o_bcw_summary_label"><t t-esc="level.label"/>:</span>
                                    <span t-if="sel" class="o_bcw_summary_value"><t t-esc="sel.code"/> · <t t-esc="sel.name"/></span>
                                    <span t-else="" class="o_bcw_summary_value o_bcw_muted">—</span>
                                </div>
                            </t>
                        </div>
                        <p t-if="classificationComplete" class="o_bcw_hint">
                            La referencia final será <code><t t-esc="state.session ? state.session.class_code : ''"/>-NN</code>,
                            con el consecutivo que se reserva al agregar cada producto.
                        </p>
                    </div>
                </div>
            </section>

            <!-- ============================================== etapa 2 -->
            <section class="o_bcw_stage" t-att-class="{o_bcw_stage_active: state.stage === 2, o_bcw_stage_locked: !stageReachable(2)}">
                <header class="o_bcw_stage_head" t-on-click="() => this.goStage(2)">
                    <span class="o_bcw_num o_bcw_tone_brand">2</span>
                    <div class="o_bcw_stage_titles">
                        <h2>Busca y agrega productos</h2>
                        <p>Busca productos y agrégalos a la clasificación seleccionada.</p>
                    </div>
                    <div class="o_bcw_stage_mini" t-if="state.collapsed[2] and lines.length">
                        <span class="o_bcw_chip"><t t-esc="lines.length"/> agregados</span>
                    </div>
                    <button class="o_bcw_collapse" t-att-class="{o_bcw_collapse_open: !state.collapsed[2]}"
                            t-on-click.stop="() => this.toggleCollapse(2)"
                            t-att-title="state.collapsed[2] ? 'Desplegar etapa 2' : 'Colapsar etapa 2'">
                        <i class="fa fa-angle-left"/>
                    </button>
                </header>

                <div class="o_bcw_stage_body" t-if="!state.collapsed[2]">
                    <div t-if="!stageReachable(2)" class="o_bcw_empty">Completa la clasificación de la etapa 1 para buscar productos.</div>
                    <t t-else="">
                        <div class="o_bcw_searchbar">
                            <i class="fa fa-search o_bcw_search_icon" aria-hidden="true"/>
                            <input type="text" class="o_input o_bcw_input o_bcw_search_input" t-ref="productSearch"
                                   t-att-value="state.search.query" t-att-readonly="confirmed"
                                   aria-label="Buscar productos" aria-describedby="bcw_search_help" autocomplete="off"
                                   placeholder="Buscar por nombre, referencia o código..."
                                   t-on-input="onSearchInput" t-on-keydown="onSearchKeydown"/>
                            <button class="o_bcw_scan" t-att-class="{o_bcw_scan_on: state.scan}" t-on-click="toggleScan"
                                    t-att-aria-pressed="state.scan ? 'true' : 'false'" aria-label="Modo lector de código de barras"
                                    title="Lector de código de barras: al leer un código con un resultado único, se agrega solo.">
                                <i class="fa fa-barcode"/>
                            </button>
                        </div>

                        <p id="bcw_search_help" class="o_bcw_hint mb-2">Flechas para seleccionar; Enter para agregar. Los productos agregados se consultan en la etapa 3.</p>
                        <div class="o_bcw_table_wrap" t-ref="searchResults" role="region" aria-label="Resultados de búsqueda" tabindex="0" t-att-aria-busy="state.search.loading ? 'true' : 'false'">
                            <table class="o_list_table table-sm o_bcw_table">
                                <thead>
                                    <tr>
                                        <th>Producto</th>
                                        <th class="o_bcw_w_ref">Referencia</th>
                                        <th class="o_bcw_w_brand">Marca</th>
                                        <th class="o_bcw_w_act">Acción</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr t-foreach="state.search.records" t-as="record" t-key="record.id" t-att-data-product-id="record.id"
                                        t-att-class="{o_bcw_row_selected: state.search.selectedId === record.id}">
                                        <td class="o_bcw_cell_name">
                                            <t t-esc="record.name"/>
                                            <span t-if="record.locked_by" class="o_bcw_badge o_bcw_badge_warn" t-att-title="'En clasificación en curso: ' + record.locked_by">En clasificación</span>
                                            <span t-if="record.reclassify_from and !record.locked_by" class="o_bcw_badge o_bcw_badge_danger" t-att-title="'Ya clasificado con la clave ' + record.reclassify_from + '. Agregarlo aquí le genera una clave nueva.'">Otra clasificación</span>
                                        </td>
                                        <td class="o_bcw_mono"><t t-esc="record.default_code or record.reference or '—'"/></td>
                                        <td><t t-esc="record.brand or '—'"/></td>
                                        <td class="o_bcw_w_act">
                                            <button t-if="!record.locked_by" class="o_bcw_add" t-on-click="() => this.addProduct(record)"
                                                    t-att-disabled="confirmed or state.busy or state.search.loading" t-att-aria-label="'Agregar ' + record.name" title="Agregar a la clasificación">
                                                <i class="fa fa-plus"/> Agregar
                                            </button>
                                            <span t-else="" class="o_bcw_hint" t-att-title="record.locked_by">En otra sesión</span>
                                        </td>
                                    </tr>
                                    <tr t-if="!state.search.records.length">
                                        <td colspan="4" class="o_bcw_empty">
                                            <t t-if="state.search.loading">Buscando...</t>
                                            <t t-else="">Sin resultados para esa búsqueda.</t>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>

                        <div class="o_bcw_pager">
                            <span class="o_bcw_pager_text" role="status" aria-live="polite">
                                Mostrando <t t-esc="pageFrom"/> a <t t-esc="pageTo"/> de <t t-esc="state.search.total"/> resultados
                            </span>
                            <div class="o_bcw_pager_btns">
                                <button class="o_bcw_icon_btn" t-att-disabled="!hasPrevPage or state.search.loading or state.busy" t-on-click="prevPage" aria-label="Página anterior"><i class="fa fa-angle-left"/></button>
                                <button class="o_bcw_icon_btn" t-att-disabled="!hasNextPage or state.search.loading or state.busy" t-on-click="nextPage" aria-label="Página siguiente"><i class="fa fa-angle-right"/></button>
                            </div>
                        </div>
                    </t>
                </div>
            </section>

            <!-- ============================================== etapa 3 -->
            <section class="o_bcw_stage" t-att-class="{o_bcw_stage_active: state.stage === 3, o_bcw_stage_locked: !stageReachable(3)}">
                <header class="o_bcw_stage_head" t-on-click="() => this.goStage(3)">
                    <span class="o_bcw_num o_bcw_tone_classifier">3</span>
                    <div class="o_bcw_stage_titles">
                        <h2>Actualiza nombres y unidades de producto</h2>
                        <p>Revisa el nombre y la unidad de cada producto antes de generar las claves.</p>
                    </div>
                    <div class="o_bcw_stage3_meta" t-if="classificationComplete">
                        <div class="o_bcw_meta_line">
                            <span class="o_bcw_meta_label">Clasificación:</span>
                            <span class="o_bcw_code o_bcw_code_sm">
                                <t t-foreach="codeSegments" t-as="seg" t-key="seg_index">
                                    <span t-attf-class="o_bcw_seg o_bcw_tone_{{seg.tone}}" t-esc="seg.text"/>
                                    <span t-if="!seg_last" class="o_bcw_dash">-</span>
                                </t>
                            </span>
                        </div>
                        <div class="o_bcw_meta_line o_bcw_muted" role="status" aria-live="polite">Productos agregados: <strong t-esc="lines.length"/></div>
                    </div>
                </header>

                <div class="o_bcw_stage_body">
                    <div t-if="confirmed" class="o_bcw_done">
                        <i class="fa fa-check-circle"/>
                        <div>
                            <strong>Clasificación aplicada.</strong>
                            <span>Se escribieron <t t-esc="lines.length"/> claves definitivas en el catálogo.</span>
                        </div>
                        <button class="btn btn-sm btn-secondary" t-on-click="openProducts">Ver productos</button>
                        <button class="btn btn-sm btn-primary" t-on-click="startNew">Nueva clasificación</button>
                    </div>

                    <div t-if="!lines.length" class="o_bcw_empty">Todavía no hay productos en esta clasificación. Agrégalos desde la etapa 2.</div>

                    <t t-else="">
                        <div class="o_bcw_table_wrap" role="region" aria-label="Productos agregados" tabindex="0">
                            <table class="o_list_table table-sm o_bcw_table o_bcw_table_lines">
                                <thead>
                                    <tr>
                                        <th class="o_bcw_w_grip"/>
                                        <th class="o_bcw_w_num">#</th>
                                        <th>Nombre actual</th>
                                        <th>Nombre anterior</th>
                                        <th class="o_bcw_w_uom">Unidad de medida</th>
                                        <th class="o_bcw_w_ref2">Referencia</th>
                                        <th class="o_bcw_w_cons">Consecutivo</th>
                                        <th class="o_bcw_w_img">Imagen</th>
                                        <th class="o_bcw_w_act2">Acción</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr t-foreach="lines" t-as="line" t-key="line.id"
                                        t-att-class="{o_bcw_dragging: state.dragId === line.id, o_bcw_dragover: state.overId === line.id, o_bcw_row_reclassified: line.reclassified or line.reclassify_from}"
                                        t-on-dragover.prevent="() => this.onDragOver(line)"
                                        t-on-drop.prevent="() => this.onDrop(line)">
                                        <td class="o_bcw_w_grip">
                                            <span class="o_bcw_grip" title="Arrastra para cambiar el orden" draggable="true"
                                                  t-on-dragstart="(ev) => this.onDragStart(line, ev)" t-on-dragend="onDragEnd">
                                                <i class="fa fa-ellipsis-v"/><i class="fa fa-ellipsis-v"/>
                                            </span>
                                        </td>
                                        <td class="o_bcw_w_num o_bcw_mono"><t t-esc="line_index + 1"/></td>
                                        <td>
                                            <input type="text" class="o_bcw_input o_bcw_cell_input"
                                                   t-att-value="line.new_name" t-att-readonly="confirmed or line.preserve_reference" t-att-aria-label="'Nombre de ' + line.old_name"
                                                   t-att-title="line.preserve_reference ? 'Producto ya clasificado: el nombre y la referencia se conservan' : ''"
                                                   t-on-input="(ev) => this.onLineInput(line, 'new_name', ev)"
                                                   t-on-change="(ev) => this.onNameChange(line, ev)"/>
                                        </td>
                                        <td class="o_bcw_muted"><t t-esc="line.old_name or '—'"/></td>
                                        <td class="o_bcw_w_uom">
                                            <select class="o_bcw_input o_bcw_cell_input" t-att-disabled="confirmed or line.uom_locked" t-att-aria-label="'Unidad de ' + line.old_name"
                                                    t-att-title="line.uom_locked ? 'Tiene movimientos de inventario: conserva la unidad ' + line.product_uom_name + '. Registra cajas o bolsas como empacados.' : ''"
                                                    t-on-change="(ev) => this.onUomChange(line, ev)">
                                                <option value="" t-att-selected="!line.uom_id">—</option>
                                                <option t-foreach="state.uoms" t-as="uom" t-key="uom.id"
                                                        t-att-value="uom.id" t-att-selected="uom.id === line.uom_id" t-esc="uom.name"/>
                                            </select>
                                        </td>
                                        <td class="o_bcw_mono o_bcw_ref">
                                            <t t-esc="line.reference or '—'"/>
                                            <i t-if="line.preserve_reference" class="fa fa-lock o_bcw_lock" aria-hidden="true" title="Clave conservada: el producto ya estaba clasificado"/>
                                            <i t-if="line.reclassify_from or line.reclassified" class="fa fa-exclamation-circle o_bcw_reclass" aria-hidden="true"
                                               t-att-title="line.reclassified ? 'Cambió de clasificación: antes ' + (line.old_reference || '—') : 'Ya clasificado con la clave ' + line.reclassify_from + '. Al generar claves recibirá una nueva.'"/>
                                        </td>
                                        <td class="o_bcw_mono"><t t-esc="line.preserve_reference ? '—' : (line.consecutive_label or '—')"/></td>
                                        <td class="o_bcw_w_img">
                                            <button t-if="imageCount(line)" type="button" class="o_bcw_img_btn o_bcw_img_has"
                                                    t-att-title="imageTitle(line)" t-att-aria-label="imageTitle(line) + ' de ' + (line.new_name || line.old_name)"
                                                    t-on-click="() => this.openImages(line)">
                                                <i class="fa fa-picture-o" aria-hidden="true"/>
                                                <span t-if="imageCount(line) > 1" class="o_bcw_img_badge" t-esc="imageCount(line)"/>
                                            </button>
                                            <span t-else="" class="o_bcw_img_btn o_bcw_img_none" title="Sin imagen" role="img" aria-label="Sin imagen">
                                                <i class="fa fa-picture-o" aria-hidden="true"/>
                                            </span>
                                        </td>
                                        <td class="o_bcw_w_act2">
                                            <button class="o_bcw_icon_btn" t-on-click="() => this.editLine(line)" title="Editar producto clasificado"><i class="fa fa-pencil"/></button>
                                            <button class="o_bcw_icon_btn o_bcw_danger" t-on-click="() => this.removeLine(line)"
                                                    t-att-disabled="confirmed or state.busy" title="Quitar de la lista"><i class="fa fa-trash-o"/></button>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <p class="o_bcw_hint">
                            Puedes cambiar el orden de los productos arrastrando el icono de 6 puntos.
                            El consecutivo no cambia al reordenar: se reserva al agregar el producto.
                            <t t-if="lines.some((l) => l.preserve_reference)"> Los productos con <i class="fa fa-lock" aria-hidden="true"/> ya tenían clave de esta clasificación: conservan referencia y nombre; solo se actualizan sus demás datos.</t>
                            <t t-if="lines.some((l) => l.reclassify_from or l.reclassified)"> Los productos <span class="text-danger fw-bold">en rojo</span> tenían clave completa de otra clasificación: <t t-if="confirmed">recibieron una clave nueva.</t><t t-else="">recibirán una clave nueva y su numeración cambiará.</t></t>
                        </p>
                        <p t-if="!classificationComplete" class="o_bcw_warn">Completa la clasificación en la etapa 1 para reservar los consecutivos y poder generar las claves.</p>
                    </t>
                </div>
            </section>
        </div>

        <!-- ================================================== navegación -->
        <footer t-if="!state.loading" class="o_bcw_foot">
            <button class="btn btn-sm btn-secondary" t-att-disabled="state.stage === 1" t-on-click="previous">
                <i class="fa fa-arrow-left"/> Anterior
            </button>
            <span class="o_bcw_foot_state">Etapa <t t-esc="state.stage"/> de 3</span>
            <button t-if="state.stage &lt; 3" class="btn btn-sm btn-primary" t-on-click="next">
                Siguiente <i class="fa fa-arrow-right"/>
            </button>
            <button t-else="" class="btn btn-sm btn-primary" t-att-disabled="!lines.length or confirmed or state.busy" t-on-click="confirm">
                Generar claves <i class="fa fa-arrow-right"/>
            </button>
        </footer>
    </div>
</t>

<t t-name="biotex_catalog.LineEditorDialog">
    <Dialog size="'xl'" contentClass="'o_bcw_modal o_bcw_editor_modal'" requestClose.bind="requestClose">
        <t t-set-slot="header">
            <div class="o_bcw_modal_head">
                <div class="o_bcw_modal_head_text">
                    <h4 class="o_bcw_modal_title"><i class="fa fa-pencil-square-o"/>Editando Producto: <span class="o_bcw_modal_product" t-esc="productTitle"/></h4>
                    <div t-if="referenceSegments.length" class="o_bcw_modal_ref" aria-label="Referencia final">
                        <span class="o_bcw_code o_bcw_code_sm">
                            <t t-foreach="referenceSegments" t-as="seg" t-key="seg_index">
                                <span t-attf-class="o_bcw_seg o_bcw_tone_{{seg.tone}}" t-esc="seg.text"/>
                                <span t-if="!seg_last" class="o_bcw_dash">-</span>
                            </t>
                        </span>
                        <small class="o_bcw_help">Referencia final. La clasificación y el consecutivo no se modifican aquí.</small>
                    </div>
                    <p t-else="" class="o_bcw_modal_sub">Modifica la información del producto antes de aplicar la clasificación.</p>
                </div>
                <button class="btn-close" aria-label="Cerrar" t-on-click="requestClose"/>
            </div>
        </t>

        <div t-if="state.loading" class="o_bcw_modal_loading"><i class="fa fa-circle-o-notch fa-spin fa-2x"/></div>

        <div t-else="" class="o_bcw_modal_body o_bcw_editor_flow">
            <div t-if="errorMessages.length" class="o_bcw_error_bar" role="alert">
                <i class="fa fa-exclamation-circle" aria-hidden="true"/>
                <div class="o_bcw_error_bar_text">
                    <strong>Hay <t t-esc="errorMessages.length"/> dato(s) por corregir.</strong>
                    <span t-esc="errorMessages[0]"/>
                </div>
                <button type="button" class="btn btn-sm btn-outline-danger" t-on-click="scrollToFirstError">Ir al campo</button>
            </div>

            <!-- ============================================== datos principales -->
            <section class="o_bcw_editor_block" aria-label="Datos principales">
                <div class="o_bcw_editor_heading">
                    <h5>Datos principales</h5>
                </div>
                <div class="o_bcw_field">
                    <label class="o_bcw_label" for="bcw_editor_name">Nuevo Nombre <span class="o_bcw_req">*</span></label>
                    <textarea id="bcw_editor_name" t-ref="editorName" class="o_input o_bcw_input o_bcw_textarea o_bcw_name_input" rows="3" required="required"
                              t-att-aria-invalid="!!state.errors.new_name" t-att-class="{ 'o_bcw_input--error': state.errors.new_name }"
                              t-att-readonly="props.readonly or nameLocked" placeholder="Nombre con el que quedará el producto en el catálogo"
                              t-att-value="state.draft.new_name" t-on-input="(ev) => this.onInput('new_name', ev)"/>
                    <small t-if="nameLocked" class="o_bcw_help"><i class="fa fa-lock me-1" aria-hidden="true"/>Producto ya clasificado con esta clave: el nombre y la referencia se conservan. Puedes editar el resto de los datos.</small>
                    <small t-if="state.errors.new_name" class="o_bcw_error" t-esc="state.errors.new_name"/>
                    <div t-if="showSuggestion and !props.readonly" class="o_bcw_suggestion" role="status">
                        <span class="o_bcw_help">Con medidas y complemento: <strong t-esc="suggestedName"/></span>
                        <button type="button" class="btn btn-link btn-sm p-0" t-on-click="useSuggestedName">Usar esta descripción</button>
                    </div>
                </div>
                <div class="o_bcw_grid o_bcw_grid--2">
                    <div class="o_bcw_field">
                        <label class="o_bcw_label" for="bcw_editor_uom">Unidad indivisible <span class="o_bcw_req">*</span></label>
                        <select id="bcw_editor_uom" class="o_input o_bcw_input" required="required" t-att-aria-invalid="!!state.errors.uom_id" t-att-class="{ 'o_bcw_input--error': state.errors.uom_id }"
                                t-att-disabled="props.readonly or state.line.uom_locked" t-on-change="(ev) => this.onSelect('uom_id', ev)">
                            <option value="" t-att-selected="!state.draft.uom_id">—</option>
                            <option t-foreach="state.catalogs.uoms" t-as="uom" t-key="uom.id" t-att-value="uom.id"
                                    t-att-selected="uom.id === state.draft.uom_id" t-esc="uom.name"/>
                        </select>
                        <small t-if="state.line.uom_locked" class="o_bcw_help"><i class="fa fa-lock me-1" aria-hidden="true"/>El producto ya tiene movimientos de inventario en <strong t-esc="state.line.product_uom_name"/>: esa unidad se conserva. Registra cajas, bolsas u otros empaques como empacados con su equivalencia.</small>
                        <small t-else="" class="o_bcw_help">Una misma unidad para comprar, vender y contar. Las cajas se registran como empacados.</small>
                        <small t-if="state.errors.uom_id" class="o_bcw_error" t-esc="state.errors.uom_id"/>
                    </div>
                </div>
            </section>

            <!-- ============================================== empacados y códigos -->
            <section class="o_bcw_editor_block" aria-label="Empacados de productos y códigos de barras">
                <div class="o_bcw_editor_heading"><h5>Empacados de productos y códigos de barras</h5><p>Un renglón por cada tipo de empaque: indica cuántas unidades indivisibles contiene y su código de barras.</p></div>
                <div class="table-responsive"><table class="table table-sm o_bcw_rows"><thead><tr><th class="o_bcw_col_type">Tipo de empaque</th><th>Cantidad<t t-if="baseUomName"> (<t t-esc="baseUomName"/>)</t></th><th>Código de barras</th><th/></tr></thead><tbody>
                    <tr t-foreach="state.draft.presentation_data" t-as="row" t-key="row_index">
                        <td>
                            <select aria-label="Tipo de empaque" class="o_bcw_input" t-att-disabled="props.readonly" t-on-change="(ev) => this.onRow('presentation_data', row_index, 'package_type_id', ev)">
                                <option value="" t-att-selected="!row.package_type_id" t-esc="rowTypeLabel(row) || '— Elige el tipo —'"/>
                                <option t-foreach="state.catalogs.package_types" t-as="pt" t-key="pt.id" t-att-value="pt.id"
                                        t-att-selected="pt.id === row.package_type_id" t-esc="pt.name"/>
                            </select>
                        </td>
                        <td>
                            <div class="o_bcw_qty_unit">
                                <input aria-label="Cantidad por empacado" class="o_bcw_input" type="number" min="1" step="1" t-att-value="row.quantity" t-att-readonly="props.readonly" t-on-input="(ev) => this.onRow('presentation_data', row_index, 'quantity', ev)"/>
                                <span class="o_bcw_qty_unit_label" t-att-title="'Unidad indivisible: ' + (baseUomName || '—')" t-esc="baseUomName || '—'"/>
                            </div>
                        </td>
                        <td><input aria-label="Código del empacado" class="o_bcw_input o_bcw_mono" t-att-value="row.barcode" t-att-readonly="props.readonly" t-on-input="(ev) => this.onRow('presentation_data', row_index, 'barcode', ev)"/></td>
                        <td><button t-if="!props.readonly" class="btn btn-link" aria-label="Quitar empacado" t-on-click="() => this.removeRow('presentation_data', row_index)"><i class="fa fa-trash"/></button></td>
                    </tr>
                    <tr t-if="!state.draft.presentation_data.length"><td colspan="4" class="o_bcw_help">Sin empacados. La unidad indivisible se vende y cuenta por sí sola.</td></tr>
                </tbody></table></div>
                <button t-if="!props.readonly" class="btn btn-outline-secondary btn-sm align-self-start" t-on-click="() => this.addRow('presentation_data')"><i class="fa fa-plus me-1"/>Agregar empacado</button>
                <p t-if="state.errors.presentation_data" class="o_bcw_error" t-esc="state.errors.presentation_data"/>
            </section>

            <!-- ============================================== medidas -->
            <section class="o_bcw_editor_block" aria-label="Medidas por componente">
                <div class="o_bcw_editor_heading"><h5>Medidas por componente</h5><p>Opcionales, salvo que la familia las requiera. Elige el atributo dimensional del catálogo (calibre, largo, volumen…) y captura valor y unidad.</p></div>
                <div class="table-responsive">
                    <table class="table table-sm o_bcw_rows"><thead><tr><th>Componente</th><th class="o_bcw_col_type">Atributo</th><th>Valor</th><th>Unidad</th><th/></tr></thead><tbody>
                        <tr t-foreach="state.draft.measure_data" t-as="row" t-key="row_index">
                            <td><input aria-label="Componente" class="o_bcw_input" placeholder="AGUJA" t-att-value="row.component" t-att-readonly="props.readonly" t-on-input="(ev) => this.onRow('measure_data', row_index, 'component', ev)"/></td>
                            <td>
                                <select aria-label="Atributo dimensional" class="o_bcw_input" t-att-disabled="props.readonly" t-on-change="(ev) => this.onRow('measure_data', row_index, 'measure_type_id', ev)">
                                    <option value="" t-att-selected="!row.measure_type_id" t-esc="(!row.measure_type_id and row.measure_type) ? row.measure_type : '— Elige el atributo —'"/>
                                    <option t-foreach="state.catalogs.measure_types" t-as="mt" t-key="mt.id" t-att-value="mt.id" t-att-title="mt.description"
                                            t-att-selected="mt.id === row.measure_type_id" t-esc="mt.name"/>
                                </select>
                            </td>
                            <td><input aria-label="Valor" class="o_bcw_input" type="number" min="0" step="any" t-att-value="row.value" t-att-readonly="props.readonly" t-on-input="(ev) => this.onRow('measure_data', row_index, 'value', ev)"/></td>
                            <td>
                                <select aria-label="Unidad de la medida" class="o_bcw_input" t-att-disabled="props.readonly" t-on-change="(ev) => this.onRow('measure_data', row_index, 'unit_uom_id', ev)">
                                    <option value="" t-att-selected="!row.unit_uom_id" t-esc="(!row.unit_uom_id and row.unit) ? row.unit : '— Unidad —'"/>
                                    <optgroup t-if="suggestedUoms(row).length" label="Sugeridas para el atributo">
                                        <option t-foreach="suggestedUoms(row)" t-as="u" t-key="u.id" t-att-value="u.id" t-att-selected="u.id === row.unit_uom_id" t-esc="u.name"/>
                                    </optgroup>
                                    <optgroup label="Todas las unidades">
                                        <option t-foreach="otherUoms(row)" t-as="u" t-key="u.id" t-att-value="u.id" t-att-selected="u.id === row.unit_uom_id" t-esc="u.name"/>
                                    </optgroup>
                                </select>
                            </td>
                            <td><button t-if="!props.readonly" class="btn btn-link" aria-label="Quitar medida" t-on-click="() => this.removeRow('measure_data', row_index)"><i class="fa fa-trash"/></button></td>
                        </tr>
                        <tr t-if="!state.draft.measure_data.length"><td colspan="5" class="o_bcw_help">Sin medidas estructuradas.</td></tr>
                    </tbody></table>
                </div>
                <button t-if="!props.readonly" class="btn btn-outline-secondary btn-sm align-self-start" t-on-click="() => this.addRow('measure_data')"><i class="fa fa-plus me-1"/>Agregar medida</button>
                <p t-if="state.errors.measure_data" class="o_bcw_error" t-esc="state.errors.measure_data"/>
            </section>

            <!-- ============================================== información adicional -->
            <section class="o_bcw_editor_block" aria-label="Información adicional">
                <div id="bcw_editor_details" class="o_bcw_details_body">

                    <section class="o_bcw_editor_photos" aria-label="Imágenes del producto">
                        <div class="o_bcw_editor_heading o_bcw_editor_heading--sub"><h6>Imágenes del producto</h6><p>Hasta tres imágenes. Guarda los cambios y confirma la clasificación para aplicarlas al catálogo.</p></div>
                        <div class="o_bcw_photo_grid">
                            <article class="o_bcw_photo_card" t-foreach="state.line.photos || []" t-as="photo" t-key="photo.field">
                                <h6 t-esc="photo.label"/>
                                <button t-if="photoUrl(photo)" type="button" class="o_bcw_photo_preview" t-att-aria-label="'Ampliar ' + photo.label.toLowerCase()" t-on-click="() => this.previewPhoto(photo)">
                                    <img t-att-src="photoUrl(photo)" t-att-alt="photo.label"/><span><i class="fa fa-search-plus"/> Ampliar</span>
                                </button>
                                <div t-else="" class="o_bcw_photo_empty"><i class="fa fa-picture-o" aria-hidden="true"/><span>Sin imagen</span></div>
                                <small class="o_bcw_help" t-if="photoPending(photo)">Pendiente de aplicar</small>
                                <small class="o_bcw_error" t-if="photo.conflict and photoPending(photo)">La imagen del catálogo cambió. Revisa la imagen actual antes de reemplazarla.</small>
                                <t t-if="!props.readonly">
                                    <label class="btn btn-outline-secondary btn-sm o_bcw_photo_upload" t-att-for="'bcw_photo_' + photo.field" t-att-class="{ disabled: state.saving || state.readingImages }">
                                        <i class="fa fa-upload"/> <t t-esc="photoUrl(photo) ? 'Cambiar imagen' : 'Agregar imagen'"/>
                                        <input class="o_bcw_photo_input" type="file" t-att-id="'bcw_photo_' + photo.field" t-att-aria-label="'Seleccionar ' + photo.label.toLowerCase()" accept="image/jpeg,image/png,image/webp,image/gif" t-att-disabled="state.saving or state.readingImages" t-on-change="(ev) => this.selectPhoto(photo, ev)"/>
                                    </label>
                                    <button t-if="photoPending(photo)" type="button" class="btn btn-link btn-sm" t-att-disabled="state.saving or state.readingImages" t-on-click="() => this.useCurrentPhoto(photo)">Usar imagen actual</button>
                                </t>
                            </article>
                        </div>
                        <small class="o_bcw_help" role="status" t-esc="state.readingImages ? 'Leyendo imagen…' : 'JPG, PNG, WebP o GIF. Máximo 10 MB por imagen.'"/>
                    </section>

                    <!-- fila 1: fabricante + referencia del fabricante -->
                    <div class="o_bcw_grid o_bcw_grid--2">
                        <div class="o_bcw_field">
                            <label class="o_bcw_label">Fabricante</label>
                            <div t-if="state.labels.manufacturer" class="o_bcw_chip" t-att-class="{ o_bcw_chip_suggested: state.manufacturerSuggested }">
                                <i t-if="state.manufacturerSuggested" class="fa fa-magic" aria-hidden="true" title="Sugerido por la marca"/>
                                <span t-esc="state.labels.manufacturer"/>
                                <button t-if="!props.readonly" class="o_bcw_chip_x" aria-label="Cambiar fabricante" t-on-click="() => this.clearLookup('manufacturer')"><i class="fa fa-times"/></button>
                            </div>
                            <input t-else="" type="text" class="o_bcw_input" placeholder="Buscar fabricante..."
                                   t-att-readonly="props.readonly" t-on-input="(ev) => this.onLookup('manufacturer', ev)"
                                   t-on-focus="(ev) => this.onLookup('manufacturer', ev)" t-on-blur="() => this.closeLookup('manufacturer')" t-on-keydown="(ev) => this.onLookupKeydown('manufacturer', ev)"/>
                            <button t-if="!props.readonly and !state.labels.manufacturer and state.labels.manufacturerQuery" class="btn btn-link btn-sm p-0 align-self-start" t-on-click="() => this.createRelation('manufacturer')">Crear en catálogo: <t t-esc="state.labels.manufacturerQuery"/></button>
                            <div class="o_bcw_lookup" t-if="state.lookups.manufacturer.length">
                                <button t-foreach="state.lookups.manufacturer" t-as="r" t-key="r.id" class="o_bcw_lookup_item"
                                        t-on-pointerdown.prevent="noop" t-on-click="() => this.pick('manufacturer', r)" t-esc="r.name"/>
                            </div>
                            <small class="o_bcw_help" t-esc="state.manufacturerSuggested ? 'Sugerido por la marca, editable.' : 'Se sugiere a partir de la marca de la clasificación; puedes cambiarlo.'"/>
                        </div>
                        <div class="o_bcw_field">
                            <label class="o_bcw_label">Ref. del fabricante</label>
                            <input type="text" class="o_bcw_input o_bcw_mono" placeholder="AM-74375-50/40"
                                   t-att-value="state.draft.manufacturer_ref" t-att-readonly="props.readonly"
                                   t-on-input="(ev) => this.onInput('manufacturer_ref', ev)"/>
                        </div>
                    </div>

                    <!-- fila 2: modelo + distribuidor -->
                    <div class="o_bcw_grid o_bcw_grid--2">
                        <div class="o_bcw_field">
                            <label class="o_bcw_label">Modelo</label>
                            <input type="text" class="o_bcw_input" placeholder="74375-50"
                                   t-att-value="state.draft.model" t-att-readonly="props.readonly"
                                   t-on-input="(ev) => this.onInput('model', ev)"/>
                        </div>
                        <div class="o_bcw_field">
                            <label class="o_bcw_label">Distribuidor primario</label>
                            <div t-if="state.labels.distributor" class="o_bcw_chip">
                                <span t-esc="state.labels.distributor"/>
                                <button t-if="!props.readonly" class="o_bcw_chip_x" aria-label="Cambiar distribuidor" t-on-click="() => this.clearLookup('distributor')"><i class="fa fa-times"/></button>
                            </div>
                            <input t-else="" type="text" class="o_bcw_input" placeholder="Buscar proveedor..."
                                   t-att-readonly="props.readonly" t-on-input="(ev) => this.onLookup('distributor', ev)"
                                   t-on-focus="(ev) => this.onLookup('distributor', ev)" t-on-blur="() => this.closeLookup('distributor')" t-on-keydown="(ev) => this.onLookupKeydown('distributor', ev)"/>
                            <button t-if="!props.readonly and !state.labels.distributor and state.labels.distributorQuery" class="btn btn-link btn-sm p-0 align-self-start" t-on-click="() => this.createRelation('distributor')">Crear en catálogo: <t t-esc="state.labels.distributorQuery"/></button>
                            <div class="o_bcw_lookup" t-if="state.lookups.distributor.length">
                                <button t-foreach="state.lookups.distributor" t-as="r" t-key="r.id" class="o_bcw_lookup_item"
                                        t-on-pointerdown.prevent="noop" t-on-click="() => this.pick('distributor', r)" t-esc="r.name"/>
                            </div>
                        </div>
                    </div>

                    <!-- fila 3: países de origen (multi) -->
                    <div class="o_bcw_field">
                        <label class="o_bcw_label" for="bcw_editor_country">País de origen</label>
                        <div class="o_bcw_multi" t-att-class="{ 'o_bcw_multi--readonly': props.readonly }">
                            <span t-foreach="state.multi.country" t-as="c" t-key="c.id" class="o_bcw_chip o_bcw_chip--sm">
                                <span t-esc="c.name"/>
                                <button t-if="!props.readonly" class="o_bcw_chip_x" t-att-aria-label="'Quitar ' + c.name" t-on-click="() => this.removeMulti('country', c.id)"><i class="fa fa-times"/></button>
                            </span>
                            <input t-if="!props.readonly" id="bcw_editor_country" type="text" class="o_bcw_multi_input" autocomplete="off"
                                   t-att-placeholder="state.multi.country.length ? 'Agregar otro país...' : 'Buscar país...'"
                                   t-on-input="(ev) => this.onLookup('country', ev)"
                                   t-on-focus="(ev) => this.onLookup('country', ev)" t-on-blur="() => this.closeLookup('country')" t-on-keydown="(ev) => this.onLookupKeydown('country', ev)"/>
                               <i class="fa fa-caret-down o_bcw_multi_caret" aria-hidden="true"/>
                        </div>
                        <div class="o_bcw_lookup" t-if="state.lookups.country.length">
                            <button t-foreach="state.lookups.country" t-as="r" t-key="r.id" class="o_bcw_lookup_item"
                                    t-on-pointerdown.prevent="noop" t-on-click="(ev) => this.pick('country', r, ev)" t-esc="r.name"/>
                        </div>
                        <small class="o_bcw_help">Puedes indicar varios; el primero es el país principal.</small>
                    </div>

                    <!-- fila 4: equipos (multi) + especialidad -->
                    <div class="o_bcw_grid o_bcw_grid--2">
                        <div class="o_bcw_field">
                            <label class="o_bcw_label" for="bcw_editor_equipment">Equipo relacionado</label>
                            <div class="o_bcw_multi" t-att-class="{ 'o_bcw_multi--readonly': props.readonly }">
                                <span t-foreach="state.multi.equipment" t-as="e" t-key="e.id" class="o_bcw_chip o_bcw_chip--sm">
                                    <span t-esc="e.name"/>
                                    <button t-if="!props.readonly" class="o_bcw_chip_x" t-att-aria-label="'Quitar ' + e.name" t-on-click="() => this.removeMulti('equipment', e.id)"><i class="fa fa-times"/></button>
                                </span>
                                <input t-if="!props.readonly" id="bcw_editor_equipment" type="text" class="o_bcw_multi_input" autocomplete="off"
                                       t-att-placeholder="state.multi.equipment.length ? 'Agregar otro equipo...' : 'Buscar equipo...'"
                                       t-on-input="(ev) => this.onLookup('equipment', ev)"
                                       t-on-focus="(ev) => this.onLookup('equipment', ev)" t-on-blur="() => this.closeLookup('equipment')" t-on-keydown="(ev) => this.onLookupKeydown('equipment', ev)"/>
                                   <i class="fa fa-caret-down o_bcw_multi_caret" aria-hidden="true"/>
                            </div>
                            <div class="o_bcw_lookup" t-if="state.lookups.equipment.length">
                                <button t-foreach="state.lookups.equipment" t-as="r" t-key="r.id" class="o_bcw_lookup_item"
                                        t-on-pointerdown.prevent="noop" t-on-click="(ev) => this.pick('equipment', r, ev)" t-esc="r.name"/>
                            </div>
                            <small class="o_bcw_help">El primero es el equipo principal.</small>
                        </div>
                        <div class="o_bcw_field">
                            <label class="o_bcw_label" for="bcw_editor_specialty">Especialidad</label>
                            <div class="o_bcw_multi" t-att-class="{ 'o_bcw_multi--readonly': props.readonly }">
                                <span t-foreach="state.multi.specialty" t-as="sp" t-key="sp.id" class="o_bcw_chip o_bcw_chip--sm">
                                    <span t-esc="sp.name"/>
                                    <button t-if="!props.readonly" class="o_bcw_chip_x" t-att-aria-label="'Quitar ' + sp.name" t-on-click="() => this.removeMulti('specialty', sp.id)"><i class="fa fa-times"/></button>
                                </span>
                                <input t-if="!props.readonly" id="bcw_editor_specialty" type="text" class="o_bcw_multi_input" autocomplete="off"
                                       t-att-placeholder="state.multi.specialty.length ? 'Agregar otra especialidad...' : 'Buscar especialidad...'"
                                       t-on-input="(ev) => this.onLookup('specialty', ev)"
                                       t-on-focus="(ev) => this.onLookup('specialty', ev)" t-on-blur="() => this.closeLookup('specialty')" t-on-keydown="(ev) => this.onLookupKeydown('specialty', ev)"/>
                                   <i class="fa fa-caret-down o_bcw_multi_caret" aria-hidden="true"/>
                            </div>
                            <div class="o_bcw_lookup" t-if="state.lookups.specialty.length">
                                <button t-foreach="state.lookups.specialty" t-as="r" t-key="r.id" class="o_bcw_lookup_item"
                                        t-on-pointerdown.prevent="noop" t-on-click="(ev) => this.pick('specialty', r, ev)" t-esc="r.name"/>
                            </div>
                            <small class="o_bcw_help">Puedes indicar varias; la primera es la principal.</small>
                        </div>
                    </div>

                    <div class="o_bcw_field">
                        <label class="o_bcw_label">Características</label>
                        <textarea class="o_bcw_input o_bcw_textarea" rows="3" placeholder="Electrodo estéril desechable diseñado para estudios de electromiografía."
                                  t-att-readonly="props.readonly" t-on-input="(ev) => this.onInput('notes', ev)"
                                  t-esc="state.draft.notes"/>
                    </div>
                    <div class="o_bcw_field">
                        <label class="o_bcw_label">Compatibilidad</label>
                        <textarea class="o_bcw_input o_bcw_textarea" rows="3" placeholder="Equipos, marcas o modelos con los que es compatible" t-att-readonly="props.readonly" t-on-input="(ev) => this.onInput('compatibility_notes', ev)" t-esc="state.draft.compatibility_notes"/>
                    </div>
                    <div class="o_bcw_field">
                        <label class="o_bcw_label">Notas internas</label>
                        <textarea class="o_bcw_input o_bcw_textarea" rows="3" placeholder="Solo visibles para el equipo interno" t-att-readonly="props.readonly" t-on-input="(ev) => this.onInput('internal_notes', ev)" t-esc="state.draft.internal_notes"/>
                    </div>

                    <!-- datos heredados de la ficha anterior: se conservan, pero no compiten con lo importante -->
                    <details class="o_bcw_legacy">
                        <summary><i class="fa fa-history me-1" aria-hidden="true"/>Datos anteriores del producto</summary>
                        <div class="o_bcw_grid o_bcw_grid--2">
                            <div class="o_bcw_field">
                                <label class="o_bcw_label">Medidas anteriores (texto)</label>
                                <input type="text" class="o_bcw_input" placeholder="75 mm (longitud) x 0.5 mm (diámetro)"
                                       t-att-value="state.draft.measure" t-att-readonly="props.readonly"
                                       t-on-input="(ev) => this.onInput('measure', ev)"/>
                            </div>
                            <div class="o_bcw_field">
                                <label class="o_bcw_label">Texto de presentación anterior</label>
                                <input type="text" class="o_bcw_input" list="o_bcw_contents" placeholder="Pieza / Bolsa con 50"
                                       t-att-value="state.draft.content" t-att-readonly="props.readonly"
                                       t-on-input="(ev) => this.onInput('content', ev)"/>
                                <datalist id="o_bcw_contents">
                                    <option t-foreach="state.catalogs.contents" t-as="c" t-key="c" t-att-value="c"/>
                                </datalist>
                            </div>
                        </div>
                    </details>
                </div>
            </section>

        </div>

        <t t-set-slot="footer">
            <button class="btn btn-primary" t-att-disabled="props.readonly or state.saving or state.readingImages" t-on-click="save">Guardar cambios</button>
            <button class="btn btn-secondary" t-on-click="requestClose">Cancelar</button>
        </t>
    </Dialog>
</t>

<t t-name="biotex_catalog.ClassificationLeaveDialog">
    <Dialog size="'md'" title="'Cambios sin guardar'">
        <p class="mb-0">¿Deseas guardar los cambios o salir de la edición sin guardarlos?</p>
        <t t-set-slot="footer">
            <button t-if="props.canSave" type="button" class="btn btn-primary" t-on-click="save">Guardar cambios</button>
            <button type="button" class="btn btn-outline-danger" t-on-click="discard">Salir sin guardar</button>
            <button type="button" class="btn btn-secondary" t-on-click="props.close">Seguir editando</button>
        </t>
    </Dialog>
</t>

<t t-name="biotex_catalog.ClassificationPhotoPreview">
    <Dialog size="'lg'" title="props.label">
        <img class="o_bcw_photo_full" t-att-src="props.src" t-att-alt="props.label"/>
        <t t-set-slot="footer"><button type="button" class="btn btn-secondary" t-on-click="props.close">Cerrar</button></t>
    </Dialog>
</t>
</templates>
